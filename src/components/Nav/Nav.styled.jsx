import styled from '@emotion/styled';

export const Nav = styled.nav`
  display: flex;
  top: 0px;
  left: 0px;
  position: sticky;
  z-index: 1100;
  gap: 20px;
  padding: 40px;
  background-color: rgba(30, 44, 49, 0.396);
`;
